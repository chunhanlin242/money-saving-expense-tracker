{
  "name": "travel-money",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:local": "wrangler d1 execute travel-money --local --file=./schema.sql",
    "db:remote": "wrangler d1 execute travel-money --remote --file=./schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4"
  }
}
